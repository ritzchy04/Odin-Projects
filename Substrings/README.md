#Odin Projects - Substrings
Substrings methods takes a string and an array (dictionary), and returns a hash listing of each substring that was found in the original string and how many times it was found.

